const { o3 } = require('goss_proto');

let obj = o3;
let array = [];

do{
    array.push(obj.name);
    obj = Object.getPrototypeOf(obj);
}
while(obj)
console.log()
module.exports.protoArray = array;
